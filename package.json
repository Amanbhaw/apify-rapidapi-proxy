{
  "name": "apify-rapidapi-proxy",
  "version": "1.0.0",
  "description": "HTTP API proxy that exposes Apify actors as RapidAPI endpoints",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "apify-client": "^2.10.0",
    "express": "^4.21.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=20"
  }
}
